# CodexUIKit

`CodexUIKit` contains reusable AppKit UI components for Codex-native interfaces.

The initial seed is animated text transitions for updating attributed text,
including numeric glyph transitions and text attachment integration.
